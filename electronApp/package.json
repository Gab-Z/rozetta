{
  "name": "electronapp",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile": "node-gyp rebuild",
    "start": "electron .",
    "rebuild": "electron-rebuild -v 1.8.2 -f -w module"
  },
  "author": "gabz",
  "license": "ISC",
  "dependencies": {
    "bindings": "^1.3.0",
    "nan": "^2.10.0"
  },
  "devDependencies": {
    "electron-rebuild": "^1.7.3"
  }
}
